// The client's 8-page JagKalyan Tarak Gurukul booklet, to be supplied as a
// PDF and split into page images. Populate this array with the image paths
// once the pages are extracted — the FlipBook component shows a "coming
// soon" placeholder automatically while it's empty.
export const bookletPages: string[] = [];
